dataset_config:
  min_labeled_ratio: 0 
  min_bbox_percent: 0 
  skip_patch_validation: true
  targets:
    hz:
      activation: "none"
      weight: 1


auxiliary_tasks:
  distance_transform:
    type: distance_transform
    source_target: hz
    losses:
      - name: "MSELoss"
        weight: 1.0
        kwargs: {}
